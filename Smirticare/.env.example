# Copy this file to .env and fill in a real endpoint once you have one.
# Leave it unset for now — the app falls back to browser TTS automatically,
# it will not break without this.
VITE_TTS_ENDPOINT=https://your-backend.example.com/api/tts
